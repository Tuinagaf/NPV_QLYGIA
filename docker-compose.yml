version: '3.8'

services:
  web:
    build: .
    command: gunicorn qlygia.wsgi:application --bind 0.0.0.0:8000
    volumes:
      - .:/app
      - sqlite_data:/app  # Persist SQLite database
    ports:
      - "8000:8000"
    env_file:
      - .env

volumes:
  sqlite_data:
